[project]
name = "weather_app"
version = "0.1"
requires-python = ">=3.8"
dependencies = [
    "flask",
    "flask-login",
    "python-dotenv"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"